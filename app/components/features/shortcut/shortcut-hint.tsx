export const ShortcutHint = ({shortcut}: { shortcut: string }) => (
    <span className="w-5 h-5 inline-flex bg-primary text-white justify-center items-center p-2 text-xs rounded shadow">
        {shortcut}
    </span>
)
